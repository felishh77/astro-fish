---
//! It is now instead by the following in `astro.config.ts`
/*
  adapter: vercel({
    webAnalytics: {
      enabled: true,
    },
  }),
*/

/* 
    This component will be added to the end of <head>
    Here is an example to add Vercel Analytics
    tip: if you want to add Google Analytics, just add `googleAnalyticsId` to your config in `astro.config.{ts,mts,js,mjs}`
*/

// import Analytics from "@vercel/analytics/astro";
---

<!-- {import.meta.env.PROD && <Analytics />} -->
